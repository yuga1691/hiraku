rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /discordRateLimits/{docId} {
      allow read, write: if false;
    }

    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
